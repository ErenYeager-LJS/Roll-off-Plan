# SQLite 本地同步实现计划

1. 新增 Node SQLite 服务，自动建表并提供 API。
2. 新增 API 前端封装。
3. 改造前端状态加载、添加、删除、自定义食物和清空今日逻辑。
4. 新增体重录入和历史趋势图。
5. 更新 README 和 package scripts。
6. 增加服务端测试和前端结构测试。
7. 运行 `npm test`、浏览器 smoke test。
8. 提交并推送 GitHub。

